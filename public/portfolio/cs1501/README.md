# cs1501 media

> **Status — 2026-09-09.** `cs1501-questionnaire.png` has landed (clean
> full-resolution "Tell Me About You" capture, 2482×1348, uncropped). It is
> currently reused as the **homepage** preview tile for CS 1501, standing in
> for the not-yet-captured arranged-desk `cs1501-home.png`. All other assets
> below are still `source needed` — re-export them from the running course
> site (full window, uncropped) and drop them in under the canonical names.
> The detailed CS 1501 case study is intentionally not built yet.


Drop images and video for this project here. Anything in this folder is served
from `/portfolio/cs1501/<filename>`.

The empty slots on the live page show the exact filename each one expects —
match it, then replace the `slot(...)` call with a real media object. See
`public/portfolio/README.md` for the full reference.

## Planned assets

`MANIFEST.md` (human-readable) and `media.manifest.json` (machine-readable)
record the media this case study will use, what each asset demonstrates, and
the story beat it serves. The case study is about designing and teaching a
UVA course on building software with AI systems — the website and its
exercises are the evidence, not the project.

Expected files — lowercase, hyphenated, `cs1501-*` prefix:

| Filename | Beat | Status |
|---|---|---|
| `cs1501-home.png` | Hero | source needed |
| `cs1501-course-experience.png` | Course experience outside an LMS (Questions flashcards) | source needed |
| `cs1501-interactive-build.png` | Student-facing interactive exercise (Build Your AI Friend, 0/5) | source needed |
| `cs1501-api-lesson.png` | Systems / AI-integration teaching (instructions-vs-knowledge interstitial) | source needed |
| `cs1501-ai-systems.png` | Conversation → built artifact (Build Your AI Friend, 5/5) | source needed |
| `cs1501-demo.mp4` (+ `cs1501-demo-poster.jpg`) | Live walkthrough | source unconfirmed |
| `cs1501-questions-back.png` | Reserved — Questions cards flipped to answers | source needed |
| `cs1501-questionnaire.png` | Reserved — pre-class input → curriculum design | candidate located |
| `cs1501-ai-setup.png` | Reserved — pair for the API lesson (setup worksheet) | source needed |
| `cs1501-api-diagram.png` | Reserved — alternate standalone "what an AI API is" graphic | candidate, needs confirmation |

Drop original full-resolution exports straight in — do not crop, scale,
straighten, recolour, or edit the UI inside them. The real course surface is
the evidence.
