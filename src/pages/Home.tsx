import { Core } from '../sections/Core';

/**
 * The three projects ARE the homepage. No hero, no bio, no separate
 * "selected work" framing above it — the branch diagram is the whole
 * composition, centered in the viewport rather than beginning as a
 * subsection further down the page.
 */
export default function Home() {
  return <Core />;
}
